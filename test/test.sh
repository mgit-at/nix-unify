#!/bin/bash

# deploy
# check if nix-daemon overwritten
# check if nix still working
# check if nginx started
# check if mergePath applied, nix in path
# reboot
# check if new nix in use
# check if nix-unify-at-boot ran
# deploy change without nginx
# check if nginx properly removed
# run uninstall
# check if mergePath un-applied
