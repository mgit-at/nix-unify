module shareSystemd || return

proc main(; ; ctx, cfg) {
  if (cfg.enable) {
    for unit in (cfg.units) {
      setvar ctx.links.etc["systemd/system/$unit"] = { enable: true, target: "systemd/system/$unit" }
    }
  }
}
